# 部署检查清单

## 部署时间：2026年3月30日

## 一、云函数部署清单

### ✅ 已部署云函数（无需操作）
1. **userInit** - 用户初始化 ✅
2. **productMgr** - 产品管理 ✅  
3. **adminMgr** - 管理后台（基础功能）✅
4. **orderMgr** - 订单管理 ✅
5. **reviewMgr** - 评价管理 ✅
6. **resetData** - 数据重置 ✅

### 🔄 需要更新的云函数

#### 1. adminMgr/index.js - 押金审批扩展
**修改内容：**
- 新增 getDepositApplyList 方法
- 新增 approveDeposit 方法
- 新增 rejectDeposit 方法
- 新增押金审批Tab支持

**部署命令：**
```bash
tcb fn deploy adminMgr -e cloud1-3g4sjhqr5e28e54e
```

#### 2. daigouMgr/index.js - 积分退还逻辑
**修改内容：**
- cancelOrder 方法补充积分退还
- handleRefund 方法补充积分退还
- 修复 price 重复声明问题

**部署命令：**
```bash
tcb fn deploy daigouMgr -e cloud1-3g4sjhqr5e28e54e
```

#### 3. orderMgr/index.js - 订单状态管理
**修改内容：**
- getOrderList 返回前格式化 createTimeText
- 优化订单列表时间显示

**部署命令：**
```bash
tcb fn deploy orderMgr -e cloud1-3g4sjhqr5e28e54e
```

#### 4. productMgr/index.js - description字段处理
**修改内容：**
- create 方法补充 description 字段写入
- update 方法补充 description 字段写入
- 修复 description 字段缺失问题

**部署命令：**
```bash
tcb fn deploy productMgr -e cloud1-3g4sjhqr5e28e54e
```

#### 5. userInit/index.js - 用户初始化优化
**修改内容：**
- init action 返回 userInfo 补充 daigouStats/daigouLevel/isDaigouVerified
- 优化用户信息返回结构

**部署命令：**
```bash
tcb fn deploy userInit -e cloud1-3g4sjhqr5e28e54e
```

### 🆕 新增云函数（需要首次部署）

#### 1. dailyTasks/ - 定时任务（重建）
**功能：**
- autoCancelTimeout: 48小时取消订单+退还积分
- autoConfirmReceived: 14天自动确认收货
- 需要配置每2小时触发器

**部署命令：**
```bash
tcb fn deploy dailyTasks -e cloud1-3g4sjhqr5e28e54e
```

**触发器配置：**
```bash
tcb fn trigger create dailyTasks --type timer --cron "0 */2 * * *"
```

#### 2. paymentMgr/ - 支付管理
**功能：**
- 微信支付集成
- 钱包充值功能
- 积分充值功能
- 服务费从钱包扣除

**部署命令：**
```bash
tcb fn deploy paymentMgr -e cloud1-3g4sjhqr5e28e54e
```

#### 3. wallet/ - 钱包管理（可选）
**功能：**
- 钱包余额查询
- 交易记录管理

**部署命令：**
```bash
tcb fn deploy wallet -e cloud1-3g4sjhqr5e28e54e
```

## 二、数据库集合创建清单

### ✅ 已创建集合
1. **users** - 用户信息 ✅
2. **products** - 特产信息 ✅
3. **swapOrders** - 互换订单 ✅
4. **reviews** - 评价信息 ✅
5. **system_config** - 系统配置 ✅
6. **share_configs** - 分享配置 ✅
7. **daigouOrders** - 代购订单 ✅
8. **daigouReviews** - 代购评价 ✅
9. **daigouVerify** - 代购验证 ✅
10. **daigouDepositApply** - 押金申请 ✅
11. **deposit_logs** - 押金流水 ✅
12. **points_log** - 积分流水 ✅
13. **addresses** - 收货地址 ✅

### 🆕 需要创建的集合

#### 1. recharge_apply - 充值申请
**字段结构：**
```json
{
  "_id": "ObjectId",
  "userId": "string",           // 用户ID
  "amount": "number",          // 充值金额
  "status": "string",          // pending/approved/rejected
  "paymentMethod": "string",   // 支付方式
  "transactionId": "string",   // 交易ID
  "createdAt": "Date",         // 创建时间
  "updatedAt": "Date",         // 更新时间
  "approvedAt": "Date",        // 审批时间
  "approvedBy": "string",      // 审批人
  "rejectReason": "string"     // 拒绝原因
}
```

**创建命令：**
```javascript
// 在云开发控制台执行
db.createCollection("recharge_apply")
```

#### 2. wallet_logs - 钱包流水
**字段结构：**
```json
{
  "_id": "ObjectId",
  "userId": "string",           // 用户ID
  "type": "string",            // 类型：recharge/withdraw/payment/refund
  "amount": "number",          // 金额
  "balanceBefore": "number",   // 操作前余额
  "balanceAfter": "number",    // 操作后余额
  "relatedId": "string",       // 关联ID（订单ID等）
  "description": "string",     // 描述
  "createdAt": "Date"          // 创建时间
}
```

**创建命令：**
```javascript
db.createCollection("wallet_logs")
```

#### 3. invite_rewards - 邀请奖励
**字段结构：**
```json
{
  "_id": "ObjectId",
  "inviterId": "string",       // 邀请人ID
  "inviteeId": "string",       // 被邀请人ID
  "rewardType": "string",      // 奖励类型：cash/points
  "rewardAmount": "number",    // 奖励金额
  "status": "string",          // pending/paid
  "createdAt": "Date",         // 创建时间
  "paidAt": "Date"             // 支付时间
}
```

**创建命令：**
```javascript
db.createCollection("invite_rewards")
```

#### 4. disputes - 纠纷仲裁
**字段结构：**
```json
{
  "_id": "ObjectId",
  "orderId": "string",         // 订单ID
  "orderType": "string",       // 订单类型：swap/daigou
  "applicantId": "string",     // 申请人ID
  "respondentId": "string",    // 被申请人ID
  "reason": "string",          // 纠纷原因
  "evidence": "array",         // 证据列表
  "status": "string",          // pending/reviewing/resolved
  "createdAt": "Date",         // 创建时间
  "resolvedAt": "Date",        // 解决时间
  "resolution": "string",      // 解决方案
  "resolvedBy": "string"       // 解决人
}
```

**创建命令：**
```javascript
db.createCollection("disputes")
```

#### 5. wishlists - 心愿单
**字段结构：**
```json
{
  "_id": "ObjectId",
  "userId": "string",          // 用户ID
  "productId": "string",       // 产品ID
  "createdAt": "Date",         // 创建时间
  "notified": "boolean"        // 是否已通知
}
```

**创建命令：**
```javascript
db.createCollection("wishlists")
```

## 三、前端页面部署清单

### ✅ 已存在页面（无需操作）
1. **index** - 首页 ✅
2. **detail** - 详情页 ✅
3. **publish** - 发布页 ✅
4. **mine** - 我的页面 ✅
5. **admin** - 管理后台 ✅
6. **order** - 订单列表 ✅
7. **order-detail** - 订单详情 ✅
8. **review** - 评价页面 ✅
9. **user-profile** - 用户资料 ✅
10. **match** - 匹配页面 ✅
11. **invitedFriends** - 邀请好友 ✅
12. **daigou-checkout** - 代购结算 ✅
13. **daigou-order** - 代购订单 ✅
14. **daigou-order-list** - 代购订单列表 ✅
15. **daigou-level** - 代购等级 ✅

### 🆕 新增页面（需要配置）

#### 1. daigou-benefits/ - 代购权益页面
**配置：**
- 在 app.json 中添加页面路径
- 部署到小程序

**app.json 配置：**
```json
{
  "pages": [
    "pages/daigou-benefits/index"
  ]
}
```

#### 2. daigou-deposit/ - 代购押金页面
**配置：**
```json
{
  "pages": [
    "pages/daigou-deposit/index"
  ]
}
```

#### 3. deposit-apply/ - 押金申请页面
**配置：**
```json
{
  "pages": [
    "pages/deposit-apply/index"
  ]
}
```

#### 4. recharge/ - 充值页面
**配置：**
```json
{
  "pages": [
    "pages/recharge/index"
  ]
}
```

#### 5. wallet/ - 钱包页面
**配置：**
```json
{
  "pages": [
    "pages/wallet/index"
  ]
}
```

#### 6. withdrawal/ - 提现页面
**配置：**
```json
{
  "pages": [
    "pages/withdrawal/index"
  ]
}
```

#### 7. withdrawal-history/ - 提现记录页面
**配置：**
```json
{
  "pages": [
    "pages/withdrawal-history/index"
  ]
}
```

## 四、配置更新清单

### 1. 微信支付配置
**需要配置的参数：**
- 微信支付商户号（mchid）
- 微信支付API密钥（apiKey）
- 微信支付证书路径
- 支付回调URL

**位置：** paymentMgr 云函数配置

### 2. 阿里云短信配置
**当前配置：**
- AccessKey: LTAI5t8sk8ES4iAyF1Hqi32S
- 签名: 「泰兴钱哥建材商行」
- 模板: SMS_186915054

**验证状态：** ✅ 已配置

### 3. 定时任务配置
**dailyTasks 触发器：**
- 类型: timer
- Cron表达式: "0 */2 * * *" (每2小时执行一次)
- 需要手动配置

### 4. 环境变量配置
**需要检查的环境变量：**
- 云环境ID: cloud1-3g4sjhqr5e28e54e
- 数据库连接配置
- 缓存配置
- 日志配置

## 五、测试验证清单

### 1. 功能测试
- [ ] 用户注册登录测试
- [ ] 特产发布测试
- [ ] 代购功能测试
- [ ] 支付功能测试
- [ ] 钱包功能测试
- [ ] 邀请裂变测试
- [ ] 管理后台测试

### 2. 性能测试
- [ ] 首页加载性能
- [ ] 图片加载优化
- [ ] 云函数响应时间
- [ ] 数据库查询性能

### 3. 安全测试
- [ ] 权限控制测试
- [ ] 支付安全测试
- [ ] 数据加密测试
- [ ] 防作弊机制测试

### 4. 兼容性测试
- [ ] 微信小程序兼容性
- [ ] HarmonyOS兼容性
- [ ] 不同设备尺寸适配
- [ ] 不同网络环境测试

## 六、部署顺序建议

### 第一阶段：数据库准备
1. 创建新增的数据库集合
2. 初始化系统配置数据
3. 验证数据库连接

### 第二阶段：云函数部署
1. 部署更新后的云函数（adminMgr, daigouMgr等）
2. 部署新增的云函数（paymentMgr, dailyTasks等）
3. 配置定时任务触发器

### 第三阶段：前端部署
1. 更新 app.json 配置
2. 上传小程序代码
3. 提交审核（如需）

### 第四阶段：配置验证
1. 验证微信支付配置
2. 验证短信服务配置
3. 验证环境变量配置

### 第五阶段：功能测试
1. 执行功能测试清单
2. 修复发现的问题
3. 性能优化调整

## 七、回滚计划

### 回滚触发条件
1. 部署后出现严重功能故障
2. 数据丢失或损坏
3. 安全漏洞暴露
4. 性能严重下降

### 回滚步骤
1. 立即停止新功能使用
2. 回滚到上一个稳定版本
3. 恢复数据库备份
4. 通知用户系统维护

### 备份策略
1. 数据库每日自动备份
2. 代码版本控制
3. 配置文件备份
4. 用户数据备份

---

**部署负责人**: _______________  
**部署时间**: 2026年3月30日  
**部署状态**: □ 未开始 □ 进行中 □ 已完成  
**部署结果**: □ 成功 □ 部分成功 □ 失败  

**备注**: 请按照清单顺序执行部署，每个步骤完成后打勾确认。