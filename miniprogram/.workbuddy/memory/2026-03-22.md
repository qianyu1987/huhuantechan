# 2026-03-22

## Bug修复（3个）

修复了神秘特产页和发布页的3个Bug：

1. **mystery/index.js - getProvinceByCode返回值处理错误**（第63行）
   - 原：`getProvinceByCode(p.province) || p.province || '神秘'`（返回对象，导致显示`[object Object]`）
   - 改：`const prov = getProvinceByCode(p.province); prov ? prov.name : (p.province || '神秘')`

2. **mystery/index.js - 路由路径错误**
   - `goToProductDetail`：`/pages/detail/detail` → `/pages/detail/index`
   - `goToSwapDetail`：`/pages/order-detail/order-detail` → `/pages/order-detail/index`

3. **publish/index.js - 发布成功后跳转路径错误**（第309行）
   - `/pages/detail/detail?id=...` → `/pages/detail/index?id=...`
