# 2026-03-27 工作日志

## 文档生成（22:59）

- **MEMORY.md 精简**：文件超限，重写压缩至合理大小，保留全部核心信息

## 代购特产开发文档（23:44）

- **新建** `specialty-swap/docs/代购特产功能开发文档.md`：完整的代购功能开发规范，包含用户故事、数据库设计、云函数API（daigouMgr）、前端页面清单、业务规则（防超卖/超时机制/退款规则）、微信支付分账要点、三阶段开发排期
- **更新** `specialty-swap/docs/开发文档.md`：版本升至 v1.1.0，新增 2.6 代购特产功能模块、4.6 daigouMgr API 表、5.12 daigouOrders 集合设计，路由表补充3个新页面

## 核心功能开发文档（23:48）

- **新建** `specialty-swap/docs/核心功能开发文档（P0+P1）.md`：覆盖7个功能的完整开发规范：
  - P0①积分充值（微信支付v3）：recharge_orders集合、paymentMgr新云函数、充值档位（¥1/6/18/48/98）
  - P0②纠纷仲裁：disputes集合、orderMgr扩展applyDispute/replyDispute、adminMgr扩展resolveDispute
  - P0③服务通知推送：common/notifyHelper公共模块、8个推送场景矩阵、触发式订阅策略
  - P0④订单超时：dailyTasks改每2h执行、confirmed→72h取消-3分、shipped→14天自动确认
  - P1⑤邀请裂变：userInit已有骨架，users补inviteCode/invitedBy，双方+50积分/首换再+100
  - P1⑥心愿单：wishlists集合（上限5条/30天过期）、wishlistMgr新云函数、标签/省份匹配推送
  - P1⑦信用分完善：common/creditHelper公共模块、S/A/B/C/D/E/banned七级体系、完整加减分规则20条
- **更新** MEMORY.md 待办事项，细化为7个具体功能条目
- **更新** `MEMORY.md`：代购规划章节同步最新设计（含精确状态机、数据结构、云函数列表）
- **代购特产功能开发文档**：`brain/.../daigou-feature-spec.html`，包含需求背景/角色/流程/发布端/展示端/下单/数据库/云函数接口/前端清单/业务规则/优先级/风险
- **项目整体技术文档**：`brain/.../project-tech-doc.html`，包含架构图/27页面清单/云函数详情/6集合数据库设计/工具层/性能优化/部署说明/路线图



## 运营管理后台数据为空修复（16:16）

- **问题**：点击「完整后台」进入 webview，看板/用户/特产等所有模块数据为空
- **根因1**：`admin-panel.html` 中 `tryLoadFromHash()` 是 IIFE，在脚本加载阶段就执行，此时 `DOMContentLoaded` 还没触发，DOM 元素为 `null`，`injectRealData` 静默失败，但 `_realData` 已被赋值导致后续不再触发注入
- **根因2**：`index.js` 把完整列表（pageSize=20）序列化进 URL hash，JSON 后编码超过 webview URL 限制被截断
- **修复1**：`admin-panel.html` 把 `tryLoadFromHash()` 从 IIFE 改为普通函数，在 `DOMContentLoaded` 内调用（brain/0d8c4adf82454db0a75b44851b378fa0/admin-panel.html）
- **修复2**：`index.js` 每类数据裁剪为只取前10条 + 只保留必要字段，pageSize 改为10，hash 控制在8000字节内
- **待操作**：需要将 admin-panel.html 重新上传到云存储（路径 admin-panel.html）

## 云函数冷启动优化

- 新增 `cloudfunctions/common/warmup.js`：DB 连接复用模块，TTL=10分钟，热启动直接返回已有连接
- 新增 `cloudfunctions/common/cache.js`：内存 KV 缓存，支持 TTL、getOrSet 封装
- **productMgr**：引入 warmup + cache，`processImages` 函数增加临时链接内存缓存（TTL=1.5h），同一实例同一图片最多每1.5小时请求一次 getTempFileURL
- **adminMgr**：引入 warmup + cache，超管验证列表缓存10分钟，大幅减少 system_config 读取频率
- **userInit**：引入 warmup，复用 DB 连接

## 图片优化

- 新增前端工具 `miniprogram/utils/imageOptimizer.js`：
  - LRU 缓存（最多200条，TTL=110分钟）
  - `batchResolve()` 批量转临时链接（缓存命中直接返回）
  - `resolveProductCovers()` 产品封面批量处理
  - `compressImage()` 上传前客户端压缩（max 1200px，quality 80）
  - `batchUploadImages()` 批量压缩+上传
  - `handleImgError()` 图片加载失败兜底
- **index.js** 接入 imageOptimizer，替换原有无缓存的 resolveCloudUrls

## 部署手册

- 新增 `DEPLOY_MANUAL.md`：完整上线部署文档
  - 环境准备、部署前检查清单
  - 云函数部署顺序（分三批）
  - 数据库初始化（集合清单、权限配置、初始数据）
  - 前端发布流程（体验版 → 审核 → 上线）
  - 冒烟测试清单（新用户流程、核心功能、多端验证、性能验收）
  - 常见问题排查 & 回滚步骤
  - 运维监控指南（费用控制、日志查看）

## 运营数据看板

- 新增 `brain/0d8c4adf.../dashboard.html`：暗色主题运营看板
  - KPI 卡片：用户数、特产数、互换次数、好评率（带涨跌）
  - 趋势图：日活用户 + 互换成交双轴对比
  - 省份排行 TOP10（金银铜牌）
  - 品类分布（甜甜圈图 + 数据列表）
  - 用户信用等级分布（饼图 + 表格）
  - 订单状态漏斗（全流程转化）
  - 活跃时段热图（7天 × 24小时）
  - 估值区间分布柱状图
  - 6个小指标卡（DAU、留存率、待审核等）
  - 支持时间范围切换（7/30/90/365天）
  - 目前为模拟数据，接入 adminMgr getStats 后自动替换

## 完整管理后台 admin-panel.html（2026-03-27 续）

- 在原运营数据看板基础上，升级为完整的 10 模块管理系统
- **文件**: `brain/0d8c4adf.../admin-panel.html`（849行，暗色主题）
- **模块清单**:
  1. 📊 运营数据看板（原有图表迁移，支持时间范围切换）
  2. 🏮 特产管理（表格+搜索+筛选+下架/上架/删除/详情弹窗）
  3. 🔍 审核队列（卡片式布局+批量通过/拒绝+拒绝原因弹窗）
  4. 👥 用户列表（搜索+等级筛选+状态筛选+封禁/解封+信用分调整）
  5. ⭐ 信用分管理（独立管理界面，拖动滑块精确调整分值）
  6. 🤝 互换订单（表格+状态筛选+详情弹窗+强制取消）
  7. 🚨 投诉处理（卡片式，支持标记解决/处理中/封禁用户）
  8. ⚙️ 平台配置（功能开关Toggle+积分规则+信用分规则+发布限制）
  9. 📢 公告管理（新建/编辑/发布/关闭/删除公告）
  10. 🚫 黑名单（加入/解除封禁，支持时长选择）
- **通用组件**: Toast通知、分页、Modal弹窗、Badge标签
- **数据**: 模拟80用户/60特产/50订单，接入adminMgr云函数后替换


## admin-panel.html 空白修复

- **问题**: `http://localhost:7842/admin-panel.html` 打开空白
- **原因**: 页面引用了外部 CDN `cdn.jsdelivr.net/npm/echarts@5.4.3`，本地网络无法访问导致 echarts 未加载，DOMContentLoaded 中 `renderAll()` 调用 `echarts.init()` 报错，整个 JS 崩溃
- **修复**: 从 BootCDN 下载 echarts.min.js（1MB），替换 `<script src>` 标签为内联 `<script>` 内容，彻底消除 CDN 依赖
- **文件**: `brain/0d8c4adf.../admin-panel.html`，修复后文件大小约 1.1MB

## admin-panel.html 所有功能点击无效修复

- **问题**: 页面能打开但所有按钮/导航点击无反应
- **根本原因**: 第771行 `renderCreditMgr` 函数中，模板字符串内 `${}` 表达式里有分号：`${chg>=0?'var(--green)':'var(--red)';font-size:13px}` — JS引擎将分号解析为语句分隔符，导致语法错误，整个 `<script>` 块无法执行
- **修复**: 将 `font-size:13px` 移到 `${}` 外面，改为 `style="font-size:13px;color:${chg>=0?'var(--green)':'var(--red)'}"`
- **文件**: `brain/0d8c4adf.../admin-panel.html` 第771行

- 撤销 P1 阶段对 app.js 的混淆，恢复为完整可读代码
- 变量名恢复：`$pl` → `platformUtil`，`$pi` → `platformInfo`，`$ca/$tl` → `cached/cachedTTL`，`$r1~$r7` → `res`，`$rc` → `retryCount`，`$nm/$dt/$mr` → `funcName/data/maxRetries`，`$dc` → `doCall`，`$pic/$bi` → `pendingInviteCode/alreadyBound`，`$t` → `self` 等
- 代码逻辑和功能无任何变更

## 管理后台真实数据接入（2026-03-27 下午）

- **需求**: 管理后台数据都显示真实云开发数据
- **核心问题**: `pages/dashboard/index.js` 调用了不存在的 `getDashboardData` 接口
- **解决方案**:
  1. **adminMgr 新增 `getDashboardData` 接口**：查询 products/orders/reviews 集合，返回省份排行TOP10、品类分布、订单漏斗、总评价数、已完成互换数
  2. **dashboard 稳定性改进**：`Promise.all` → `Promise.allSettled`，防止单个接口失败拖垮整体
  3. **创建 `pages/dashboard/index.wxss`**：完整暗黑主题样式，包含骨架屏/KPI卡片/排行/图表/漏斗等组件
  4. **app.json 注册路由**：新增 `pages/dashboard/index`
  5. **admin 概览 tab 添加跳转入口**：`goDashboard()` 方法 + 渐变样式卡片
  6. **admin-panel.html 接入 postMessage**：`injectRealData()` 函数，收到小程序发送的真实数据后替换用户/特产/审核/订单/KPI
- **待部署**: `adminMgr` 云函数（含新增的 getDashboardData）

## admin-panel.html web-view 完整接入方案（2026-03-27 下午二）

### 架构
1. **pages/admin-webview/** 新增 web-view 容器页（约 80 行 JS）
   - `onLoad` 时并行调 6 个云函数接口（getStats/getDashboardData/getUsers/getProducts/getPendingProducts/getOrders）
   - 将数据 JSON 编码为 URL hash，拼接到 admin-panel.html 的云存储 URL 后面
   - web-view 加载 `html_url#encodedData`
2. **admin-panel.html 升级**：
   - 新增 `tryLoadFromHash()`：DOMContentLoaded 前读 URL hash 解码数据，调 `injectRealData()`
   - 新增 `loadRealDataViaCloudSDK()`：降级方案，动态加载 `@cloudbase/js-sdk`，匿名登录后直接调云函数
   - 双重数据保障：URL hash 优先，cloud SDK 兜底
3. **pages/admin/index.wxml**：超管快捷操作区增加「完整后台」按钮
4. **pages/admin/index.js**：新增 `openFullPanel()` 方法
5. **app.json**：注册 `pages/admin-webview/index`

### 部署前置条件
- 需将 admin-panel.html 上传到云存储 `cloud1-3g4sjhqr5e28e54e`
- 更新 admin-webview/index.js 第 6 行的 `ADMIN_PANEL_FILE_ID`
- 云存储文件权限设为「所有用户可读」
- 详见 `pages/admin-webview/DEPLOY.md`

## admin-panel.html FileID 更新（2026-03-27 16:06）

- **新 FileID**: `cloud://cloud1-3g4sjhqr5e28e54e.636c-cloud1-3g4sjhqr5e28e54e-1348466332/admin-panel.html`
- **旧 FileID**: `cloud://...1351909572/admin-panel.html`（已作废）
- 已更新 `pages/admin-webview/index.js` 中的 `ADMIN_PANEL_FILE_ID` 常量

## admin-panel.html 内容模块为空修复（2026-03-27 16:09）

- **问题**: 页面打开后各模块（特产/用户/订单等）内容区域为空
- **原因**: `DOMContentLoaded` 只调用了 `renderAll(30)`（看板图表），没有预渲染其他8个模块；用户必须先点击导航才能看到数据
- **修复**: 在 `DOMContentLoaded` 中新增 `renderProducts/renderReviewCards/renderUsers/renderCreditMgr/renderOrders/renderComplaints/renderAnnounces/renderBlacklist` 预渲染调用
- **图表优化**: 看板图表改用 `requestAnimationFrame` 延迟一帧初始化，确保容器有实际尺寸后再 echarts.init

## 产品功能规划文档整理（2026-03-27 16:55）

- **文件**: `brain/0d8c4adf.../product-roadmap.md`
- **状态**: 暂缓开发，留存备用
- **内容**: 15个功能点的完整规划（含变现运营层）
  - 内容情感：特产故事、产地认证徽章
  - 互换升级：心愿单、智能推荐、一对多互换、押金制度、纠纷仲裁
  - 社区UGC：互换勋章、晒单社区、评测帖、话题标签、官方科普号
  - 社交增长：邀请好友、私信聊天
  - 变现运营：积分充值、精选展位、会员订阅、特产商城、广告合作、物流整合、数据变现
- **开发优先级**: P0(押金+纠纷+积分充值+物流) → P1(邀请+心愿单+勋章) → P2(社区) → P3(推荐+一对多) → P4(商业化)

## 商业视角精简决策（2026-03-27 17:10，CTO 复盘）

**决策要点**（已更新至 product-roadmap.md）：
- **P0A 立即做**：积分充值（微信支付）+ 纠纷仲裁（人工介入）
- **P0B 延后**：押金制度（改用信用分冻结替代，降低用户门槛）；物流整合（先手动填快递号 + 复制运单查询链接）
- **P1 保留**：邀请好友（裂变）+ 心愿单（精准匹配）；互换勋章延后（等用户量）
- **P2 砍掉**：晒单社区、评测帖（独立做或等用户量 > 1万）；话题标签保留在订单评价里
- **近期并行**：订单状态推送（服务通知）+ 订单超时机制（72h未发货自动取消）+ 信用分机制完善
- **变现顺序**：积分充值 → 纠纷仲裁 → 精选展位 → 物流整合 → 会员订阅

## mine/index.js 语法错误修复（2026-03-27 22:26）

- **错误**: `Invalid shorthand property initializer` at line 353
- **原因**: 之前编辑 `_loadMyProductsPreview` 函数时，旧版本的 mysteryStyle 逻辑残留代码（353–371 行）游离在函数体外，导致 JS 解析报错
- **修复**: 直接删除 353–371 行的孤立代码片段，函数本身（320–352 行）逻辑完整无需改动
- **状态**: ✅ 已修复，lint 零报错

## 代购特产功能规划（2026-03-27 22:52）

- **背景**: 用户反馈"想要特产但没特产换"，需要纯付费代购通道
- **规划写入 MEMORY.md** `## 代购特产功能规划` 章节
- **核心要点**:
  - 发布端新增「可代购」选项（原价+服务费+库存），与互换功能并存互不影响
  - 买家直接付款，代购者48h内发货，平台抽5%服务费
  - 数据库：products集合新增`daigou`字段，新增`daigouOrders`集合
  - 前端：publish/index/detail/mine 4处改动 + 2个新页面
  - 云函数：productMgr/orderMgr 新增代购相关接口，需接入微信支付
- **优先级**: P0（发布+展示+下单支付）→ P1（发货确认+订单列表）→ P2（退款+管理后台）
- **状态**: 📋 规划完成，加入待开发列表，等待排期开发

## 代购特产需求与技术方案 HTML（2026-03-27 23:55）

- **生成** `brain/0d8c4adf.../daigou-spec.html`：完整代购功能需求与技术方案文档
- **内容**: 角色定义（代购者/买家/平台）、业务流程、订单状态机（7个状态）、数据库设计（products扩展+daigouOrders集合Schema）、云函数API（daigouMgr 9个接口+productMgr/adminMgr改造）、前端页面规划（3新增+4改动）、三阶段开发计划（P0/P1/P2共10天）、里程碑时间线
- **样式**: 橙色渐变 Hero、右侧 TOC 导航、状态机徽章、暗底代码高亮、响应式布局
