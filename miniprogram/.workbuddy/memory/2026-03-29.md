# 2026-03-29 工作日志

## 管理后台 admin/index 样式优化收尾

### 完成项
1. **第3项（统一色调/CSS变量）**：WXSS已完整，`audit-actions-row` 补充 `flex-shrink: 0` 防止窄屏被压缩
2. **第4项（Tab内容排版优化）**：
   - 等级管理Tab：搜索栏移至筛选chip前（更符合操作习惯）
   - 统一空态位置：审核(Tab4)、实名审核(Tab5)、代购管理(Tab9)、押金审批(Tab10) 的 empty-state 全部移入 list-wrap 内
   - 新增子模块底部安全区：`currentTab > 0` 时渲染 `safe-bottom`
3. 无 lint 错误

### 涉及文件
- `pages/admin/index.wxml`
- `pages/admin/index.wxss`

---

## 代购功能全面检查与修复（2026-03-29）

### 发现并修复的 Bug

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | daigouMgr/confirmReceived | 服务费费率表下标 0-6 与等级体系 1-7 不匹配 | 费率表改为 `{1:8.0, 2:7.0, ...7:4.0}` |
| 2 | daigouMgr/confirmReceived | 读取 `totalPrice` 字段不存在（订单只有 `price`）| 改为 `order.price \|\| actualPrice` |
| 3 | daigouMgr/confirmReceived | 完成订单后不更新卖家代购等级，下次计费仍用旧等级 | 用 calcDaigouLevel 重算后写入 `daigouStats.level` 和 `daigouLevel` |
| 4 | daigouMgr/applyRefund + handleRefund | 拒绝退款后状态硬编码为 `shipped`，若从 `completed` 申请则状态错误回退 | applyRefund 写入 `preRefundStatus`，handleRefund 拒绝时恢复该状态 |
| 5 | daigou-order/index.js | estimatedReward 使用 `order.price` 而非 `order.actualPrice`，积分计算偏高 | 改为 `order.actualPrice \|\| order.price` |
| 6 | daigou-order-list/index.js | pending/completed 统计数翻页后会累加导致数值虚高 | reset=true 时重算统计，翻页只更新 total |

### 已确认正常（排查后无问题）
- daigou-checkout 地址回填逻辑：address 页用 prevPage.setData 写入，onShow 检查 this.data.selectedAddress 是正确的
- detail 页底部操作栏：isMine 分支正确隔离，不会对自己商品展示购买按钮
- cancelOrder 卖家已发货判断：虽然冗余但不影响逻辑

### 涉及文件
- `cloudfunctions/daigouMgr/index.js`（待重新部署）
- `pages/daigou-order/index.js`
- `pages/daigou-order-list/index.js`
