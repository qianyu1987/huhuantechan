# 特产互换小程序 - 2026年4月3日部署清单

## 一、新增功能

### 1. 积分兑换功能 ✅
**功能描述**：用户可通过微信支付充值现金兑换积分

**新增文件**：
- `miniprogram/pages/points-exchange/index.js` - 积分兑换页面逻辑
- `miniprogram/pages/points-exchange/index.wxml` - 积分兑换页面结构
- `miniprogram/pages/points-exchange/index.wxss` - 积分兑换页面样式
- `miniprogram/pages/points-exchange/index.json` - 页面配置

**修改文件**：
- `miniprogram/app.json` - 添加积分兑换页面路径
- `miniprogram/pages/points-rule/index.js` - 添加积分兑换入口
- `miniprogram/pages/points-rule/index.wxml` - 添加积分兑换按钮
- `cloudfunctions/paymentMgr/index.js` - 添加积分兑换云函数接口

**新增数据库集合**：
- `points_exchange` - 积分兑换记录集合
- `points_logs` - 积分变动日志集合

**云函数接口**：
- `createPointsExchangeOrder` - 创建积分兑换订单
- `getPointsExchangeResult` - 查询兑换结果

---

### 2. 代购等级体系复用 ✅
**功能描述**：管理后台等级管理使用与前端一致的7级代购等级体系

**修改文件**：
- `miniprogram/pages/admin/index.wxml` - 更新等级体系展示表格
- `miniprogram/pages/admin/index.js` - 更新等级选项
- `cloudfunctions/adminMgr/index.js` - 更新等级名称和押金配置

**等级体系**（Lv.1-Lv.7）：
| 等级 | 名称 | 所需押金 | 单笔限额 |
|------|------|----------|----------|
| Lv.1 | 新手代购 | 免押金 | ¥200 |
| Lv.2 | 见习代购 | 免押金 | ¥500 |
| Lv.3 | 普通代购 | 免押金 | ¥1000 |
| Lv.4 | 资深代购 | ¥200 | ¥3000 |
| Lv.5 | 金牌代购 | ¥500 | ¥10000 |
| Lv.6 | 钻石代购 | ¥2000 | ¥50000 |
| Lv.7 | 传奇代购 | ¥5000 | 不限 |

---

### 3. 管理后台新增积分兑换管理Tab ✅
**功能描述**：超管可在后台查看所有积分兑换记录

**修改文件**：
- `miniprogram/pages/admin/index.wxml` - 添加积分兑换管理页面
- `miniprogram/pages/admin/index.js` - 添加积分兑换数据和方法
- `miniprogram/pages/admin/index.js` - 更新Tab列表

---

## 二、Bug修复

### 1. 推荐特产匹配规则弹窗显示不完整 ✅
**问题**：匹配规则弹窗内容显示不全
**修复**：
- `miniprogram/pages/match/index.wxss`
  - 添加`min-height: 50vh`确保弹窗最小高度
  - 添加`width: 100%`确保宽度正确
  - 优化`rules-body`的`max-height`和`overflow-y`

### 2. "我要拿去换"显示非当前用户产品 ✅
**问题**：match页面的"我要拿去换"显示了其他用户的产品
**修复**：
- `cloudfunctions/productMgr/index.js`
  - 修复`myList`查询逻辑，只查询当前用户的特产
  - 移除错误的`db.command.or`用法，改用直接`_openid`匹配

---

## 三、需要部署的云函数

### 必须部署
1. **paymentMgr** - 支付管理云函数
   - 新增积分兑换接口
   - 路径：`cloudfunctions/paymentMgr/index.js`

2. **productMgr** - 特产管理云函数
   - 修复我的特产查询逻辑
   - 路径：`cloudfunctions/productMgr/index.js`

3. **adminMgr** - 管理员功能云函数
   - 更新代购等级体系
   - 路径：`cloudfunctions/adminMgr/index.js`

---

## 四、需要创建的数据库集合

### 新集合
1. **points_exchange** - 积分兑换记录
   ```json
   {
     "_openid": "用户openid",
     "orderNo": "订单号",
     "points": 1000,
     "amount": 10.00,
     "status": "pending/success/failed",
     "wechatPayOrderNo": "微信支付订单号",
     "transactionId": "微信交易号",
     "createTime": "创建时间",
     "updateTime": "更新时间"
   }
   ```

2. **points_logs** - 积分变动日志
   ```json
   {
     "_openid": "用户openid",
     "type": "exchange",
     "flow": "income/expense",
     "title": "积分兑换",
     "points": 1000,
     "balanceBefore": 0,
     "balanceAfter": 1000,
     "relatedId": "订单号",
     "remark": "备注",
     "createTime": "创建时间"
   }
   ```

---

## 五、部署步骤

### 1. 创建数据库集合
```javascript
// 在云开发控制台数据库中创建以下集合：
// 1. points_exchange
// 2. points_logs（如果尚未创建）
```

### 2. 部署云函数
```bash
# 在微信开发者工具中：
# 1. 右键 cloudfunctions/paymentMgr - 创建并部署：云端安装依赖
# 2. 右键 cloudfunctions/productMgr - 创建并部署：云端安装依赖
# 3. 右键 cloudfunctions/adminMgr - 创建并部署：云端安装依赖
```

### 3. 上传小程序代码
```bash
# 在微信开发者工具中：
# 1. 点击"上传"按钮
# 2. 填写版本号：1.x.x
# 3. 填写项目备注：2026-04-03更新
# 4. 上传
```

---

## 六、功能验证清单

### 积分兑换功能
- [ ] 积分兑换页面正常显示
- [ ] 预设积分选项可点击
- [ ] 自定义积分输入正常
- [ ] 微信支付调起正常
- [ ] 支付成功后积分到账
- [ ] 积分规则页面有兑换入口

### 代购等级
- [ ] 管理后台等级管理Tab显示正确
- [ ] 等级体系表格与前端一致
- [ ] 调整等级功能正常
- [ ] 用户卡片显示所需押金

### Bug修复
- [ ] 匹配规则弹窗显示完整
- [ ] "我要拿去换"只显示当前用户产品

---

## 七、Git提交记录

```bash
# 提交信息
git add .
git commit -m "feat: 2026-04-03 功能更新

- 新增积分兑换功能（现金充值兑换积分）
- 代购等级体系复用到管理后台
- 管理后台新增积分兑换管理Tab
- 修复推荐特产匹配规则弹窗显示问题
- 修复'我要拿去换'显示非当前用户产品问题"

git push origin main
```

---

**部署日期**：2026-04-03
**部署人**：WorkBuddy AI
**版本号**：v1.x.x
